services:
  - type: web
    name: fastapi-backend
    env: python
    pythonVersion: 3.10.10
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    plan: free
    region: frankfurt
